<div class="main-card mb-3 card">
    <div class="card-header">
        <h3 class="card-title">Inicio de sesión</h3>
    </div>
    <div class="card-body">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <%= f.label :email, "Correo electrónico", class: 'form-label' %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
        </div>

        <div class="field">
          <%= f.label :password, "Contraseña", class: 'form-label' %><br />
          <%= f.password_field :password, autocomplete: "current-password", class: 'form-control' %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me, "Recordar mi inicio de sesión", class: 'form-label' %>
          </div>
        <% end %>

        <div class="actions">
          <%= f.submit "Enviar", class: 'btn btn-primary float-end' %>
        </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
</div>

<script>
  $("#new_persona").validate({
    rules: {
      "persona[email]": {
        required: true,
        email: true
      },
      "persona[password]" : {
        required: true
      }
    }
  });
</script>